{
  "name": "matrix-magic",
  "version": "1.0.0",
  "description": "library for manipulating matrices",
  "main": "build/index.js",
  "scripts": {
    "dev": "watch 'npm run build' src",
    "build": "babel src -d build",
    "test": "jest",
    "test:watch": "npm test -- --watch",
    "coverage": "jest --coverage"
  },
  "babel": {
    "presets": ["latest"]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/trainorpj/matrix-magic"
  },
  "keywords": ["matrix"],
  "author": "PJ Trainor",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-latest": "^6.24.1",
    "jest": "^20.0.4",
    "watch": "^1.0.2"
  }
}
